let numero = parserInt(prompt ("Digite um número:"));
let soma = 0;

// * variavel que vai utilizar
// * até qual número ela vai
// * incremento
for (let i = 1; i <= numero; i++) {
    soma = soma + i;
}
console.log (soma);
